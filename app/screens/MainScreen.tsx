import AppView from "@/components/views/appView";
import { MainScreenPropsType } from "@/consts/Types";

const MainScreen = ({ navigation }: MainScreenPropsType) => {
  return <AppView></AppView>;
};

export default MainScreen;
